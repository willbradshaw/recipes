[project]
name = "recipes"
version = "0.1.0"
description = "Standardized recipe markdown → PDF pipeline"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["scripts/tests"]
pythonpath = ["scripts"]
